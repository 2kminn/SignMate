# Use Python 3.11 on macOS. TensorFlow/MediaPipe wheels are not available for
# every newest CPython release, so Python 3.13/3.14 can fail during install.
numpy>=1.26,<2.0
opencv-python>=4.9
mediapipe>=0.10.14
pandas>=2.1
scikit-learn>=1.3
tensorflow>=2.15
pillow>=10.0
